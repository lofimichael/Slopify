{
	"name": "slopify",
	"module": "index.ts",
	"type": "module",
	"scripts": {
		"start": "prettier --write . && live-server --open=index.html",
		"prepare": "husky install",
		"format": "prettier --write ."
	},
	"devDependencies": {
		"@types/bun": "latest",
		"emoji-regex": "^10.4.0",
		"husky": "^9.1.7"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	},
	"dependencies": {
		"@stripe/stripe-js": "^5.4.0",
		"live-server": "^1.2.2",
		"stripe": "^17.5.0",
		"three": "^0.171.0"
	}
}

